# SupportFlow Visual Builder

A React + Vite prototype for a visual chatbot decision-tree editor.

## Project setup

- `npm install`
- `npm run dev`

## Description

This app is a lightweight builder shell for a visual flow editor. It includes:

- Responsive sidebar, canvas, and properties panel layout
- TailwindCSS styling
- Clean folder structure for components, pages, data, hooks, and utils
